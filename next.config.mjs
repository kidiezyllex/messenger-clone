/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "utfs.io",
      "static.designboom.com",
      "upload.wikimedia.org",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
